# Thermal Camera Redux GUI 使用说明书

本文说明 Windows GUI 里每个功能的作用。这个软件用于 UTi260B/兼容热成像相机，温度计算来自相机回传的热矩阵，伪彩、插值、滤波、AI 超分主要影响显示画面，不会改写 RAW 热数据。

## 推荐启动流程

1. 插入热成像相机。
2. 打开 `Thermal-Camera-Redux-GUI.exe`。
3. `设备序号` 默认先用 `0`；如果画面不是热成像设备，再换序号后重新启动。
4. 点 `启动`。
5. 刚启动读数可能不稳，等约 5 秒再看温度或做黑体校准。
6. 需要校准时，用等温黑体或均匀温度目标遮住镜头，输入 `黑体目标 C`，点 `黑体校准`。

## 基础连接

| 控件 | 作用 | 什么时候调 |
|---|---|---|
| 语言 | 切换中文/英文界面 | 看个人习惯 |
| 设备序号 | 选择 OpenCV/Media Foundation 看到的摄像头编号 | 选错摄像头、打开的是电脑自带摄像头时 |
| 预设方案 | 一键套用常见场景参数 | PCB、空调、人体、低噪声、高对比、原始显示 |
| 旋转 | 旋转热成像显示方向 | 相机 USB 口朝向变化、画面横竖不对时 |
| 启动 | 启动热成像主程序 | 开始使用 |
| 停止 | 停止热成像主程序 | 换设备序号或结束使用 |
| 运行中复位 | 让主程序恢复运行时默认状态 | 参数调乱了但不想关程序 |
| 默认参数 | 恢复 GUI 默认参数 | 想重新从干净设置开始 |
| 说明书 | 打开本文档 | 不确定某个功能用途时 |

## 显示与超分

| 控件 | 作用 | 代价/注意 |
|---|---|---|
| 软件超分倍率 | 把热图显示放大到 1x/2x/3x/4x | 放大只改善显示尺寸，不增加真实测温点 |
| AI 超分 | 使用 CPU AI 模型做 x2 显示超分，支持 FSRCNN/ESPCN | CPU 推理有性能代价；实时优先通常选 ESPCN |
| 插值算法 | 经典软件放大算法：最近邻、双线性、双三次、Lanczos | 最近邻最快但块状明显；Lanczos 更细腻但更耗时 |
| 伪彩色 | 把温度映射成颜色 | 热铁红、Jet、Turbo、灰度等只是显示风格 |
| 对比/增益 | 增强热图明暗差异 | 太高会让画面看起来更刺激，但不代表温差变大 |
| 全屏 | 主热图窗口全屏显示 | 演示或远距离观察时使用 |

## 滤波与画质

| 控件 | 作用 | 什么时候用 |
|---|---|---|
| 模糊 | 基础平滑，减少块状和噪声 | 画面太碎时 |
| 高斯滤波 | 常规空间平滑 | 噪声偏多且边缘要求不高时 |
| 双边滤波 | 保边平滑，尽量保留边缘 | 想降噪又不想糊掉边界时 |
| 时域降噪 | 多帧融合，降低随机抖动 | 相机固定、画面稳定时；运动时可能拖影 |
| 锐化 | 增强边缘 | 画面太软时；过强会放大噪声 |

## 温度与校准

| 控件 | 作用 | 什么时候用 |
|---|---|---|
| 温度单位 | 摄氏度/华氏度显示切换 | 国内一般用摄氏度 |
| 温度偏移 C | 给所有显示温度加固定补偿 | 已知整体偏高/偏低时 |
| 温漂 C/分钟 | 给显示温度加随时间线性变化的补偿 | 只在你确认存在稳定线性漂移时使用 |
| 黑体目标 C | 输入黑体或等温目标的真实摄氏温度 | 手动黑体校准前设置 |
| 黑体校准 | 读取中心 ROI 平均温度，自动计算并更新温度偏移 | 相机自带校准不准、需要用黑体做一点评估时 |
| 极值阈值 C | 影响极值/标尺相关显示阈值 | 想过滤小温差抖动时 |
| 手动温度范围 | 固定伪彩映射的最低/最高温度 | 想跨时间比较颜色，不想自动范围跳动时 |
| 最小/最大 C | 手动温度范围的上下限 | 配合手动温度范围使用 |

黑体校准说明：校准会使用当前中心矩形 ROI 的平均温度，计算 `目标温度 - 当前测量平均值` 并折算到 `温度偏移 C`。如果 `温漂 C/分钟` 正在生效，软件会把当前温漂贡献折算进新的 offset，并把 drift 清零，避免校准后温度继续漂。

## ROI、等温和标尺

| 控件 | 作用 | 什么时候用 |
|---|---|---|
| ROI 模式 | 关闭、中心点测温、中心矩形 ROI | 看中心区域温度或做黑体校准时 |
| ROI 大小 | 中心矩形 ROI 占画面的百分比 | 黑体覆盖越均匀，可以用越大的 ROI |
| 等温/过温高亮 | 高亮超过阈值的区域 | 找热点、过温点 |
| 阈值 C | 等温/过温高亮的温度阈值 | 按被测对象安全温度设置 |
| 直方图均衡 | 增强灰度/低对比画面的层次 | 只作为显示增强，不用于精确温度判断 |
| 标尺 | 显示单点、十字、水平、垂直、双向标尺 | 比较某条线或某个点的温度变化 |

## 操作按钮和输出

| 控件 | 作用 | 输出 |
|---|---|---|
| 截图+RAW | 保存当前显示 PNG 和原始 RAW | PNG 便于看图，RAW 便于后处理 |
| 连拍间隔秒 | 设置定时连拍间隔，范围为 1～86400 秒 | 例如填 1、10 或 60 |
| 开始连拍 | 立即保存第一张，之后按设定间隔持续保存 PNG | 每次开始会建立独立会话目录 |
| 停止连拍 | 停止定时保存，不关闭相机 | 已保存图片会保留 |
| 打开目录 | 打开当前连拍会话目录；尚未连拍时打开总目录 | 目录位于程序旁边的 `timelapse` |
| 录像 | 调用原有 AVI 录像路径 | 用于记录观察过程 |
| 启动命令 | 显示 GUI 当前会启动的命令行 | 排查参数、复现实验 |
| 状态栏 | 显示启动、同步、校准等状态 | 黑体校准结果会显示在这里 |

定时连拍说明：

1. 先启动相机并确认画面正常。
2. 在 `连拍间隔秒` 输入间隔，例如 `1`、`10` 或 `60`。
3. 点 `开始连拍`。软件会立即保存第一张，之后按间隔继续保存。
4. 点 `停止连拍` 结束；相机仍保持运行。
5. 每次连拍保存到 `timelapse/session-年月日-时分秒-编号`。PNG 文件名为 `thermal-年月日-时分秒-六位顺序号.png`，按名称排序就是拍摄顺序。
6. 连拍 PNG 不写入左上角运行信息框，避免平均温度、超分模式、插值算法和帧率遮挡热图；测温点、ROI、等温线等测量图层仍按界面开关保存。
7. 热图窗口默认隐藏运行信息框。需要临时查看时，在热图窗口按 `h` 循环显示；信息框已压缩为平均温度/色图、超分链路、帧率与温度修正三行。

定时连拍只保存当前显示画面的 PNG，不自动保存 RAW，防止长时间连拍迅速占满磁盘。需要保留某一时刻的原始热矩阵时，另外点一次 `截图+RAW`。

## 常见建议

- 实时 CPU 场景优先用 `ESPCN x2 CPU 实时`；如果帧率不够，先关 AI 超分，再降低软件超分倍率。
- 温度判断优先看摄氏度、ROI、标尺和 RAW 数据；伪彩只是显示方案。
- 黑体校准前等相机稳定约 5 秒；刚启动的异常高温读数不要拿来校准。
- 如果系统相机应用打开失败，不代表本软件不能用；本软件走热成像 RAW/上位机处理流程。
